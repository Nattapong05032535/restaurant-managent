# 🔧 สิ่งที่ต้องทำที่ Google Sheets และ Google Cloud

**สำหรับอีเมล:** `nattapong05032535@gmail.com`

---

## 📝 สรุปสั้นๆ

คุณต้องทำ **3 สิ่ง** ที่ Google:

### 1. สร้าง Service Account ที่ Google Cloud Console
- เปิดใช้งาน Google Sheets API
- สร้าง Service Account และดาวน์โหลด JSON key

### 2. สร้าง Google Sheet
- สร้าง Spreadsheet ใหม่
- คัดลอก Spreadsheet ID จาก URL

### 3. แชร์ Google Sheet ให้ Service Account
- แชร์ Sheet ให้อีเมลของ Service Account ด้วยสิทธิ์ Editor

---

## 🚀 ขั้นตอนละเอียด

### ขั้นตอนที่ 1: Google Cloud Console (ประมาณ 10 นาที)

1. **ไปที่:** https://console.cloud.google.com/
2. **เข้าสู่ระบบด้วย:** `nattapong05032535@gmail.com`
3. **สร้างโปรเจกต์ใหม่:**
   - คลิก dropdown "Select a project" (มุมขวาบน)
   - คลิก "New Project"
   - ตั้งชื่อ: `restaurant-management`
   - คลิก "Create"

4. **เปิดใช้งาน Google Sheets API:**
   - ไปที่ "APIs & Services" > "Library"
   - ค้นหา "Google Sheets API"
   - คลิก "Enable"

5. **สร้าง Service Account:**
   - ไปที่ "APIs & Services" > "Credentials"
   - คลิก "+ CREATE CREDENTIALS" > "Service account"
   - **ขั้นตอนที่ 1:** Service account details
     - Service account name: `restaurant-api-service`
     - คลิก "CREATE AND CONTINUE"
   - **ขั้นตอนที่ 2:** Grant this service account access to project
     - **ถ้าเห็นตัวเลือก Role:** เลือก **"Editor"** (ถ้าไม่มีก็ไม่เป็นไร)
     - **ถ้าไม่เห็นตัวเลือก Role:** ไม่เป็นไร ข้ามไปได้เลย
     - คลิก "CONTINUE" หรือ "SKIP" (ถ้ามี)
   - **ขั้นตอนที่ 3:** Grant users access to this service account
     - **Permissions (optional)** - **ไม่ต้องทำอะไร** ข้ามขั้นตอนนี้ได้
     - คลิก "DONE"
   
   **💡 หมายเหตุ:** การเลือก Role ไม่จำเป็นสำหรับการใช้งาน Google Sheets API สิ่งที่สำคัญคือการแชร์ Google Sheet ให้ Service Account
   
   **⚠️ ถ้าไม่เห็น Role ให้เลือก:** ไม่เป็นไร! ข้ามไปทำขั้นตอนต่อไปได้เลย

6. **ดาวน์โหลด JSON Key:**
   - คลิกที่ Service Account ที่สร้าง
   - ไปที่แท็บ "KEYS"
   - คลิก "ADD KEY" > "Create new key"
   - เลือก "JSON"
   - คลิก "CREATE"
   - **ไฟล์ JSON จะถูกดาวน์โหลด** (เก็บไว้!)

7. **บันทึกข้อมูลจาก JSON:**
   - เปิดไฟล์ JSON ที่ดาวน์โหลดมา
   - คัดลอกค่า `client_email` (นี่คืออีเมลของ Service Account)
   - คัดลอกค่า `private_key`

---

### ขั้นตอนที่ 2: Google Sheets (ประมาณ 2 นาที)

1. **ไปที่:** https://sheets.google.com/
2. **เข้าสู่ระบบด้วย:** `nattapong05032535@gmail.com`
3. **สร้าง Spreadsheet ใหม่:**
   - คลิก "Blank"
   - ตั้งชื่อ: `Restaurant Management`

4. **คัดลอก Spreadsheet ID:**
   - ดูที่ URL บนเบราว์เซอร์
   - URL จะเป็น: `https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit`
   - **คัดลอกส่วน SPREADSHEET_ID** (ระหว่าง `/d/` และ `/edit`)

---

### ขั้นตอนที่ 3: แชร์ Google Sheet (ประมาณ 1 นาที)

1. **เปิด Google Sheet** ที่สร้างไว้
2. **คลิกปุ่ม "Share"** (มุมขวาบน)
3. **ใส่อีเมล Service Account:**
   - ใส่อีเมลจาก JSON file (`client_email`)
   - ตัวอย่าง: `restaurant-api-service@restaurant-management.iam.gserviceaccount.com`
4. **เลือกสิทธิ์:** "Editor"
5. **ยกเลิกการติ๊ก** "Notify people"
6. **คลิก "Share"**

**✅ สำเร็จ!** ตอนนี้ Service Account สามารถเข้าถึง Google Sheet ได้แล้ว

---

## 🔗 ขั้นตอนต่อไป: ตั้งค่าในโปรเจกต์

หลังจากทำครบ 3 ขั้นตอนแล้ว ต้องตั้งค่าในโปรเจกต์:

```bash
# ใช้ helper script (แนะนำ)
./scripts/setup-google-sheets.sh ~/Downloads/restaurant-management-480003-b2746a5781f5.json
restaurant-management-480003-b2746a5781f5.json
# หรือดูคู่มือละเอียด
cat SETUP_GOOGLE_SHEETS.md
```

---

## ✅ Checklist

ทำที่ Google:
- [ ] สร้างโปรเจกต์ใน Google Cloud Console
- [ ] เปิดใช้งาน Google Sheets API
- [ ] สร้าง Service Account
- [ ] ดาวน์โหลด JSON key file
- [ ] สร้าง Google Sheet
- [ ] คัดลอก Spreadsheet ID
- [ ] แชร์ Google Sheet ให้ Service Account

ทำที่โปรเจกต์:
- [ ] ตั้งค่าไฟล์ `.env`
- [ ] รัน `pnpm run create-sheets`

---

## 📖 ดูคู่มือละเอียด

- **`SETUP_GOOGLE_SHEETS.md`** - คู่มือแบบละเอียดพร้อมภาพประกอบ

---

**🎯 เริ่มต้นที่ Google Cloud Console ได้เลย!**

