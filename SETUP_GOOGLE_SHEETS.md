# 📋 คู่มือตั้งค่า Google Sheets สำหรับ Restaurant Management System

**อีเมลที่ใช้:** `nattapong05032535@gmail.com`

---

## 🎯 ภาพรวม

คู่มือนี้จะช่วยคุณตั้งค่า Google Sheets API เพื่อใช้เป็นฐานข้อมูลสำหรับระบบจัดการร้านอาหาร โดยใช้ Service Account ที่สร้างจากอีเมล `nattapong05032535@gmail.com`

---

## 📝 ขั้นตอนที่ 1: สร้าง Google Cloud Project

### 1.1 เข้าสู่ Google Cloud Console
1. ไปที่ [Google Cloud Console](https://console.cloud.google.com/)
2. ใช้บัญชี Google ที่ผูกกับ `nattapong05032535@gmail.com` เข้าสู่ระบบ
3. ถ้ายังไม่มีโปรเจกต์ ให้สร้างโปรเจกต์ใหม่:
   - คลิกที่ dropdown "Select a project" (มุมขวาบน)
   - คลิก "New Project"
   - ตั้งชื่อโปรเจกต์: `restaurant-management` (หรือชื่อที่ต้องการ)
   - คลิก "Create"

### 1.2 เปิดใช้งาน Google Sheets API
1. ไปที่ **APIs & Services** > **Library**
2. ค้นหา "Google Sheets API"
3. คลิกที่ "Google Sheets API"
4. คลิกปุ่ม **"Enable"** (เปิดใช้งาน)
5. รอสักครู่จน API เปิดใช้งานเสร็จ

---

## 🔑 ขั้นตอนที่ 2: สร้าง Service Account

### 2.1 สร้าง Service Account
1. ไปที่ **APIs & Services** > **Credentials**
2. คลิก **"+ CREATE CREDENTIALS"** ที่ด้านบน
3. เลือก **"Service account"**
4. ใส่รายละเอียด:
   - **Service account name:** `restaurant-api-service`
   - **Service account ID:** จะสร้างอัตโนมัติ (เช่น `restaurant-api-service@restaurant-management.iam.gserviceaccount.com`)
   - **Description:** `Service account for Restaurant Management System`
5. คลิก **"CREATE AND CONTINUE"**
6. **ขั้นตอนที่ 2: Grant this service account access to project**
   - ในส่วน "Select a role" เลือก **"Editor"** (หรือ "Owner" สำหรับการทดสอบ)
   - **⚠️ สำคัญ:** นี่คือส่วนที่เลือก Role ไม่ใช่ส่วน "Permissions (optional)" ที่เป็นขั้นตอนถัดไป
   - คลิก **"CONTINUE"**
7. **ขั้นตอนที่ 3: Grant users access to this service account**
   - ส่วน "Permissions (optional)" - **ไม่ต้องทำอะไร** ข้ามขั้นตอนนี้ได้
   - คลิก **"DONE"**

### 2.2 สร้างและดาวน์โหลด JSON Key
1. ในหน้า Credentials ให้คลิกที่ Service Account ที่เพิ่งสร้าง
2. ไปที่แท็บ **"KEYS"**
3. คลิก **"ADD KEY"** > **"Create new key"**
4. เลือก **"JSON"**
5. คลิก **"CREATE"**
6. ไฟล์ JSON จะถูกดาวน์โหลดอัตโนมัติ (ชื่อไฟล์จะเป็น `restaurant-management-xxxxx.json`)

**⚠️ สำคัญ:** เก็บไฟล์ JSON นี้ไว้ในที่ปลอดภัย และอย่าอัปโหลดลง GitHub!

### 2.3 คัดลอกข้อมูลจาก JSON
เปิดไฟล์ JSON ที่ดาวน์โหลดมา คุณจะเห็นข้อมูลแบบนี้:

```json
{
  "type": "service_account",
  "project_id": "restaurant-management",
  "private_key_id": "...",
  "private_key": "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n",
  "client_email": "restaurant-api-service@restaurant-management.iam.gserviceaccount.com",
  "client_id": "...",
  ...
}
```

**บันทึกข้อมูลต่อไปนี้:**
- `client_email` → นี่คืออีเมลของ Service Account (ใช้แชร์ Google Sheet)
- `private_key` → นี่คือ Private Key (ใส่ในไฟล์ .env)

---

## 📊 ขั้นตอนที่ 3: สร้าง Google Sheet

### 3.1 สร้าง Google Sheet ใหม่
1. ไปที่ [Google Sheets](https://sheets.google.com/)
2. ใช้บัญชี `nattapong05032535@gmail.com` เข้าสู่ระบบ
3. คลิก **"Blank"** เพื่อสร้าง Spreadsheet ใหม่
4. ตั้งชื่อ Spreadsheet: `Restaurant Management`

### 3.2 คัดลอก Spreadsheet ID
จาก URL ของ Google Sheet:
```
https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit
```

**คัดลอกค่า SPREADSHEET_ID** (ส่วนที่อยู่ระหว่าง `/d/` และ `/edit`)

ตัวอย่าง:
- URL: `https://docs.google.com/spreadsheets/d/1a2b3c4d5e6f7g8h9i0j/edit`
- Spreadsheet ID: `1a2b3c4d5e6f7g8h9i0j`

**บันทึก Spreadsheet ID นี้ไว้** (จะต้องใช้ในไฟล์ .env)

### 3.3 แชร์ Google Sheet ให้ Service Account
1. ใน Google Sheet ที่สร้างไว้ คลิกปุ่ม **"Share"** (มุมขวาบน)
2. ในช่อง "Add people and groups" ให้ใส่:
   - อีเมลของ Service Account (`client_email` จากไฟล์ JSON)
   - ตัวอย่าง: `restaurant-api-service@restaurant-management.iam.gserviceaccount.com`
3. เลือกสิทธิ์เป็น **"Editor"**
4. **ยกเลิกการติ๊ก** "Notify people" (ไม่ต้องแจ้งเตือน)
5. คลิก **"Share"**

**⚠️ สำคัญ:** ต้องแชร์ Sheet ให้ Service Account ก่อน ไม่งั้น API จะไม่สามารถเข้าถึงได้!

---

## ⚙️ ขั้นตอนที่ 4: ตั้งค่าไฟล์ .env

### 4.1 สร้างไฟล์ .env
1. กลับมาที่โปรเจกต์ `restaurant-managent`
2. สร้างไฟล์ `.env` ในโฟลเดอร์ root (ถ้ายังไม่มี)

### 4.2 ใส่ข้อมูลใน .env
เปิดไฟล์ `.env` และใส่ข้อมูลดังนี้:

```env
# Google Sheets API Configuration
GOOGLE_SERVICE_ACCOUNT_EMAIL=restaurant-api-service@restaurant-management.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"
GOOGLE_SPREADSHEET_ID=your-spreadsheet-id-here

# API Configuration
PORT=3001

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001
```

**วิธีเติมข้อมูล:**

1. **GOOGLE_SERVICE_ACCOUNT_EMAIL:**
   - ใส่ค่า `client_email` จากไฟล์ JSON
   - ตัวอย่าง: `restaurant-api-service@restaurant-management.iam.gserviceaccount.com`

2. **GOOGLE_PRIVATE_KEY:**
   - ใส่ค่า `private_key` จากไฟล์ JSON
   - **สำคัญ:** ต้องครอบด้วยเครื่องหมาย `"` (double quotes)
   - ต้องใช้ `\n` แทน newline ในทุกบรรทัด
   - ตัวอย่าง:
   ```env
   GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC...\n-----END PRIVATE KEY-----\n"
   ```
   
   **วิธีแปลง Private Key ให้ถูกต้อง:**
   - เปิดไฟล์ JSON และคัดลอกค่า `private_key` ทั้งหมด
   - เปลี่ยนทุก newline (`\n`) เป็น `\\n` (backslash + n)
   - หรือใช้สคริปต์ช่วยแปลง (ดูด้านล่าง)

3. **GOOGLE_SPREADSHEET_ID:**
   - ใส่ Spreadsheet ID ที่คัดลอกไว้ในขั้นตอนที่ 3.2

### 4.3 ตัวอย่างไฟล์ .env ที่สมบูรณ์

```env
# Google Sheets API Configuration
GOOGLE_SERVICE_ACCOUNT_EMAIL=restaurant-api-service@restaurant-management.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC1234567890abcdefghijklmnopqrstuvwxyz\nABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUV\n-----END PRIVATE KEY-----\n"
GOOGLE_SPREADSHEET_ID=1a2b3c4d5e6f7g8h9i0j1234567890

# API Configuration
PORT=3001

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001
```

---

## 🔧 ขั้นตอนที่ 5: Initialize Google Sheets

หลังจากตั้งค่าไฟล์ `.env` แล้ว ต้อง initialize Google Sheets เพื่อสร้าง Sheet และ Header ที่จำเป็น

### 5.1 ติดตั้ง Dependencies (ถ้ายังไม่ติดตั้ง)
```bash
cd api
pnpm install
# หรือ
npm install
```

### 5.2 Initialize Sheets
```bash
# วิธีที่ 1: ใช้ script
pnpm run create-sheets
# หรือ
npm run create-sheets

# วิธีที่ 2: ใช้ init script
pnpm run init-sheets
# หรือ
npm run init-sheets
```

คำสั่งนี้จะ:
- ✅ สร้าง Sheets ที่จำเป็น: `Stock`, `Menu`, `Orders`
- ✅ เพิ่ม Header rows ให้อัตโนมัติ
- ✅ แสดงสถานะว่าสำเร็จหรือไม่

### 5.3 ตรวจสอบผลลัพธ์
1. กลับไปที่ Google Sheet ของคุณ
2. ควรจะเห็น Sheets ใหม่:
   - **Stock** - มี header: ชื่อสินค้า, จำนวน, ราคาต่อจำนวน, ผู้เพิ่มสต็อก, วันที่สร้าง
   - **Menu** - มี header: ชื่ออาหาร, วัตถุดิบ, ราคาขาย, วันที่สร้าง
   - **Orders** - มี header: รายการอาหาร, ยอดขาย, ต้นทุน, กำไร, วันที่สร้าง

---

## ✅ ขั้นตอนที่ 6: ตรวจสอบการตั้งค่า

### 6.1 ใช้ Check Setup Script
```bash
cd api
pnpm run check-setup
# หรือ
npm run check-setup
```

Script นี้จะตรวจสอบ:
- ✅ Environment variables ครบถ้วน
- ✅ เชื่อมต่อ Google Sheets ได้หรือไม่
- ✅ Sheets ถูกต้องหรือไม่

### 6.2 ทดสอบการเชื่อมต่อ
```bash
# รัน backend
cd api
pnpm run dev
# หรือ
npm run dev
```

ถ้าทุกอย่างถูกต้อง จะเห็นข้อความ:
```
✅ Server running on port 3001
✅ Connected to Google Sheets
```

---

## 🚀 ขั้นตอนที่ 7: รันแอปพลิเคชัน

### วิธีที่ 1: ใช้ Docker Compose (แนะนำ)
```bash
# จาก root directory
docker-compose up --build
```

### วิธีที่ 2: รันแบบ Development

**Terminal 1 - Backend:**
```bash
cd api
pnpm run dev
# หรือ
npm run dev
```

**Terminal 2 - Frontend:**
```bash
cd ui
pnpm run dev
# หรือ
npm run dev
```

### วิธีที่ 3: ใช้ Setup Script
```bash
chmod +x setup.sh
./setup.sh
```

---

## 🌐 เข้าถึงแอปพลิเคชัน

- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:3001
- **Health Check:** http://localhost:3001/health

---

## ❌ แก้ไขปัญหาที่พบบ่อย

### ปัญหา: "Unable to parse range"
**สาเหตุ:** Sheets ยังไม่ถูกสร้างหรือชื่อไม่ถูกต้อง

**แก้ไข:**
1. ตรวจสอบว่า Sheets: `Stock`, `Menu`, `Orders` ถูกสร้างแล้วใน Google Sheet
2. รัน `pnpm run create-sheets` อีกครั้ง

---

### ปัญหา: "The caller does not have permission"
**สาเหตุ:** ไม่ได้แชร์ Google Sheet ให้ Service Account

**แก้ไข:**
1. เปิด Google Sheet
2. คลิก "Share"
3. ใส่อีเมล Service Account (`client_email` จากไฟล์ JSON)
4. เลือกสิทธิ์ "Editor"
5. คลิก "Share"

---

### ปัญหา: "API not enabled"
**สาเหตุ:** ยังไม่เปิดใช้งาน Google Sheets API

**แก้ไข:**
1. ไปที่ [Google Cloud Console](https://console.cloud.google.com/)
2. APIs & Services > Library
3. ค้นหา "Google Sheets API"
4. คลิก "Enable"

---

### ปัญหา: "Invalid private key"
**สาเหตุ:** Private key ในไฟล์ .env ไม่ถูกต้อง

**แก้ไข:**
1. ตรวจสอบว่า Private key ครอบด้วย `"` (double quotes)
2. ตรวจสอบว่าใช้ `\n` สำหรับ newline
3. ตรวจสอบว่า Private key มี `-----BEGIN PRIVATE KEY-----` และ `-----END PRIVATE KEY-----`

---

### ปัญหา: Frontend ไม่แสดงข้อมูล
**สาเหตุ:** Backend ไม่ทำงานหรือ CORS error

**แก้ไข:**
1. ตรวจสอบว่า Backend รันที่ port 3001
2. ตรวจสอบว่า `NEXT_PUBLIC_API_URL=http://localhost:3001` ในไฟล์ .env
3. ตรวจสอบ logs ของ Backend

---

## 📞 ข้อมูลติดต่อ

**อีเมลที่ใช้:** `nattapong05032535@gmail.com`

หากพบปัญหาที่แก้ไขไม่ได้:
1. ตรวจสอบ logs ของ Backend
2. รัน `pnpm run check-setup` เพื่อตรวจสอบการตั้งค่า
3. ดูเอกสารเพิ่มเติมใน `SETUP.md` หรือ `QUICK_START.md`

---

## ✅ Checklist สรุป

- [ ] สร้าง Google Cloud Project แล้ว
- [ ] เปิดใช้งาน Google Sheets API แล้ว
- [ ] สร้าง Service Account แล้ว
- [ ] ดาวน์โหลด JSON key แล้ว
- [ ] สร้าง Google Sheet แล้ว
- [ ] แชร์ Google Sheet ให้ Service Account แล้ว
- [ ] สร้างไฟล์ `.env` และใส่ข้อมูลครบถ้วนแล้ว
- [ ] รัน `pnpm run create-sheets` สำเร็จแล้ว
- [ ] รัน `pnpm run check-setup` ผ่านแล้ว
- [ ] แอปพลิเคชันรันได้แล้ว

---

**🎉 เมื่อทำครบทุกขั้นตอนแล้ว แอปพลิเคชันจะพร้อมใช้งาน!**

