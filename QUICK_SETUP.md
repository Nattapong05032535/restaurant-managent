# 🚀 คู่มือติดตั้งแบบเร็ว (Quick Setup Guide)

**สำหรับอีเมล:** `nattapong05032535@gmail.com`

---

## 📋 สรุปขั้นตอน

1. ✅ สร้าง Google Cloud Service Account
2. ✅ สร้าง Google Sheet
3. ✅ ตั้งค่าไฟล์ `.env`
4. ✅ Initialize Google Sheets
5. ✅ รันแอปพลิเคชัน

---

## 🎯 วิธีที่ 1: ใช้ Helper Script (แนะนำ - ง่ายที่สุด)

### ขั้นตอนที่ 1: ดาวน์โหลด Service Account JSON Key

1. ไปที่ [Google Cloud Console](https://console.cloud.google.com/)
2. สร้าง Service Account (ดูรายละเอียดใน `SETUP_GOOGLE_SHEETS.md`)
3. ดาวน์โหลด JSON key file

### ขั้นตอนที่ 2: สร้าง Google Sheet

1. ไปที่ [Google Sheets](https://sheets.google.com/)
2. สร้าง Spreadsheet ใหม่
3. ตั้งชื่อ: `Restaurant Management`
4. คัดลอก Spreadsheet ID จาก URL

### ขั้นตอนที่ 3: ใช้ Helper Script

```bash
# ให้สิทธิ์ execute
chmod +x scripts/setup-google-sheets.sh

# รัน script (แทนที่ <json-file-path> ด้วย path ของ JSON file)
./scripts/setup-google-sheets.sh ~/Downloads/restaurant-management-xxxxx.json
```

Script จะถาม Spreadsheet ID และสร้างไฟล์ `.env` ให้อัตโนมัติ!

### ขั้นตอนที่ 4: แชร์ Google Sheet

1. เปิด Google Sheet ที่สร้างไว้
2. คลิก "Share"
3. ใส่อีเมล Service Account (จะแสดงใน script output)
4. เลือกสิทธิ์ "Editor"
5. คลิก "Share"

### ขั้นตอนที่ 5: Initialize Sheets

```bash
cd api
pnpm install  # ถ้ายังไม่ติดตั้ง
pnpm run create-sheets
```

### ขั้นตอนที่ 6: รันแอปพลิเคชัน

```bash
# วิธีที่ 1: ใช้ Docker (แนะนำ)
docker-compose up --build

# วิธีที่ 2: รันแยก
# Terminal 1
cd api && pnpm run dev

# Terminal 2
cd ui && pnpm run dev
```

---

## 🔧 วิธีที่ 2: ตั้งค่าด้วยตนเอง

### ขั้นตอนที่ 1-2: เหมือนวิธีที่ 1

### ขั้นตอนที่ 3: ตั้งค่าไฟล์ .env

```bash
# Copy template
cp env.template .env

# แก้ไข .env โดยใช้ข้อมูลจาก JSON file
```

**หรือใช้ helper script แปลง private key:**
```bash
node scripts/convert-private-key.js ~/Downloads/restaurant-management-xxxxx.json
```

### ขั้นตอนที่ 4-6: เหมือนวิธีที่ 1

---

## ⚡ ใช้ Setup Script แบบอัตโนมัติ

```bash
# ให้สิทธิ์ execute
chmod +x setup.sh

# รัน setup script
./setup.sh
```

Script จะ:
- ✅ สร้างไฟล์ `.env`
- ✅ ติดตั้ง dependencies
- ✅ พยายาม initialize Google Sheets (ถ้ามี credentials)

---

## 📖 ดูรายละเอียดเพิ่มเติม

- **คู่มือตั้งค่า Google Sheets แบบละเอียด:** `SETUP_GOOGLE_SHEETS.md`
- **Quick Start Guide:** `QUICK_START.md`
- **Full Setup Guide:** `SETUP.md`

---

## ✅ Checklist

- [ ] Service Account สร้างแล้ว
- [ ] JSON key ดาวน์โหลดแล้ว
- [ ] Google Sheet สร้างแล้ว
- [ ] แชร์ Google Sheet ให้ Service Account แล้ว
- [ ] ไฟล์ `.env` ตั้งค่าแล้ว
- [ ] รัน `pnpm run create-sheets` สำเร็จแล้ว
- [ ] แอปพลิเคชันรันได้แล้ว

---

## 🆘 มีปัญหาหรือไม่?

ดูใน `SETUP_GOOGLE_SHEETS.md` ส่วน "แก้ไขปัญหาที่พบบ่อย"

---

**🎉 เมื่อทำครบทุกขั้นตอนแล้ว แอปพลิเคชันจะพร้อมใช้งาน!**

เข้าถึงได้ที่: http://localhost:3000

