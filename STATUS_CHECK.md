# 📋 สรุปสถานะโปรเจกต์และสิ่งที่ต้องทำ

**วันที่ตรวจสอบ:** $(date)

---

## ✅ สิ่งที่ทำเสร็จแล้ว

- [x] โครงสร้างโปรเจกต์พร้อม (API + UI)
- [x] มีไฟล์ `.env` อยู่แล้ว
- [x] มีไฟล์ `env.template` สำหรับอ้างอิง
- [x] มีคู่มือและเอกสารครบถ้วน
- [x] มี Docker compose configuration

---

## ❌ สิ่งที่ยังต้องทำ

### 1. ติดตั้ง Dependencies (สำคัญมาก!)

**Backend (API):**
```bash
cd api
pnpm install
```
⚠️ **สถานะ:** ยังไม่ได้ติดตั้ง (พบ socket timeout จากการติดตั้ง)
- แก้ไข: ไฟล์ `.npmrc` ถูกสร้างแล้วเพื่อเพิ่ม timeout
- ต้องรัน `pnpm install` อีกครั้ง

**Frontend (UI):**
```bash
cd ui
pnpm install
```
⚠️ **สถานะ:** ยังไม่ได้ติดตั้ง

**คำสั่งติดตั้งทั้งหมด:**
```bash
# ใช้ Makefile (แนะนำ)
make install

# หรือติดตั้งแยก
cd api && pnpm install
cd ui && pnpm install
```

---

### 2. ตรวจสอบและตั้งค่าไฟล์ `.env`

ไฟล์ `.env` ต้องมีค่าต่อไปนี้:

```env
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
GOOGLE_SPREADSHEET_ID=your-spreadsheet-id-here
PORT=3001
NEXT_PUBLIC_API_URL=http://localhost:3001
```

**ตรวจสอบ:**
```bash
cd api
pnpm run check-setup
```

**ถ้ายังไม่ได้ตั้งค่า:**
- ดูคู่มือใน `WHAT_TO_DO_AT_GOOGLE.md`
- หรือ `SETUP_GOOGLE_SHEETS.md`
- หรือใช้ script: `./scripts/setup-google-sheets.sh <json-file>`

---

### 3. ตั้งค่า Google Sheets

ทำตาม `WHAT_TO_DO_AT_GOOGLE.md`:

#### 3.1 สร้าง Service Account ที่ Google Cloud
- [ ] สร้างโปรเจกต์ใน Google Cloud Console
- [ ] เปิดใช้งาน Google Sheets API
- [ ] สร้าง Service Account
- [ ] ดาวน์โหลด JSON key file

#### 3.2 สร้าง Google Sheet
- [ ] สร้าง Spreadsheet ใหม่
- [ ] คัดลอก Spreadsheet ID จาก URL

#### 3.3 แชร์ Google Sheet
- [ ] แชร์ Sheet ให้ Service Account email (จาก JSON) ด้วยสิทธิ์ Editor

---

### 4. Initialize Google Sheets

หลังจากตั้งค่า `.env` และแชร์ Sheet แล้ว:

```bash
cd api
pnpm run create-sheets
```

คำสั่งนี้จะ:
- สร้าง Sheets: `Stock`, `Menu`, `Orders` (ถ้ายังไม่มี)
- เพิ่ม header rows ให้อัตโนมัติ

---

### 5. ตรวจสอบการตั้งค่า

```bash
cd api
pnpm run check-setup
```

ต้องเห็น:
- ✅ Environment Variables: ทั้งหมด Set
- ✅ Connected to spreadsheet
- ✅ Sheets ทั้ง 3 แผ่นมีอยู่

---

### 6. รันแอปพลิเคชัน

#### วิธีที่ 1: ใช้ Docker (แนะนำ)

```bash
docker-compose up --build
```

#### วิธีที่ 2: รันแบบ Development

```bash
# Terminal 1 - Backend
cd api
pnpm run dev

# Terminal 2 - Frontend  
cd ui
pnpm run dev
```

#### วิธีที่ 3: ใช้ Makefile

```bash
make dev
```

---

## 🎯 ขั้นตอนสรุป (ทำตามลำดับ)

### ขั้นตอนที่ 1: แก้ปัญหา Dependencies

```bash
# 1. ตรวจสอบว่า .npmrc มี timeout settings แล้ว
cat api/.npmrc

# 2. ติดตั้ง dependencies สำหรับ API
cd api
pnpm install

# 3. ติดตั้ง dependencies สำหรับ UI
cd ../ui
pnpm install
```

**ถ้ายังมี timeout:**
- ตรวจสอบอินเทอร์เน็ต
- ลองใช้ npm แทน: `npm install`
- หรือลองใช้ registry อื่น

---

### ขั้นตอนที่ 2: ตั้งค่า Google Sheets

**ถ้ายังไม่ได้ตั้งค่า Google Sheets:**

1. อ่าน `WHAT_TO_DO_AT_GOOGLE.md`
2. ทำตามขั้นตอนที่ Google
3. ใช้ script helper:
   ```bash
   ./scripts/setup-google-sheets.sh ~/Downloads/your-json-file.json
   ```

**ถ้าตั้งค่าแล้ว:**
```bash
# ตรวจสอบว่า .env ถูกต้อง
cd api
pnpm run check-setup
```

---

### ขั้นตอนที่ 3: Initialize Sheets

```bash
cd api
pnpm run create-sheets
```

---

### ขั้นตอนที่ 4: ตรวจสอบ

```bash
cd api
pnpm run check-setup
```

ต้องเห็นทุกอย่างเป็น ✅

---

### ขั้นตอนที่ 5: รัน

```bash
# ใช้ Docker
docker-compose up --build

# หรือรันแบบ dev
make dev
```

---

## 🔍 ตรวจสอบสถานะปัจจุบัน

รันคำสั่งเหล่านี้เพื่อดูสถานะ:

```bash
# ตรวจสอบ dependencies
echo "=== API Dependencies ==="
ls -la api/node_modules 2>/dev/null && echo "✅ Installed" || echo "❌ Not installed"

echo "=== UI Dependencies ==="
ls -la ui/node_modules 2>/dev/null && echo "✅ Installed" || echo "❌ Not installed"

# ตรวจสอบ .env
echo "=== .env File ==="
test -f .env && echo "✅ Exists" || echo "❌ Missing"

# ตรวจสอบการตั้งค่า Google Sheets (ต้องติดตั้ง dependencies ก่อน)
echo "=== Google Sheets Setup ==="
cd api && pnpm run check-setup 2>/dev/null || echo "⚠️  Cannot check (dependencies not installed)"
```

---

## 📝 Checklist สรุป

### ก่อนรันแอปพลิเคชัน ต้องมี:

- [ ] **Dependencies ติดตั้งแล้ว**
  - [ ] `api/node_modules` มีอยู่
  - [ ] `ui/node_modules` มีอยู่

- [ ] **ไฟล์ .env ตั้งค่าแล้ว**
  - [ ] `GOOGLE_SERVICE_ACCOUNT_EMAIL`
  - [ ] `GOOGLE_PRIVATE_KEY`
  - [ ] `GOOGLE_SPREADSHEET_ID`

- [ ] **Google Sheets Setup**
  - [ ] Service Account สร้างแล้ว
  - [ ] Google Sheet สร้างแล้ว
  - [ ] แชร์ Sheet ให้ Service Account แล้ว

- [ ] **Google Sheets Initialized**
  - [ ] รัน `pnpm run create-sheets` สำเร็จ
  - [ ] มี Sheets: Stock, Menu, Orders
  - [ ] Headers ถูกต้อง

- [ ] **ตรวจสอบ Setup**
  - [ ] `pnpm run check-setup` ผ่านทั้งหมด

---

## 🚀 เมื่อพร้อมแล้ว

เปิดเบราว์เซอร์ไปที่:
- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:3001

---

## 📚 เอกสารที่เกี่ยวข้อง

- `WHAT_TO_DO_AT_GOOGLE.md` - สิ่งที่ต้องทำที่ Google
- `SETUP_GOOGLE_SHEETS.md` - คู่มือตั้งค่า Google Sheets แบบละเอียด
- `QUICK_START.md` - คู่มือเริ่มต้นเร็ว
- `START_HERE.md` - เริ่มต้นที่นี่

---

## ⚠️ ปัญหาที่อาจเจอ

### 1. Socket Timeout ตอนติดตั้ง dependencies
**แก้ไข:** 
- ไฟล์ `.npmrc` ถูกสร้างแล้ว
- ลองรัน `pnpm install` อีกครั้ง
- หรือลองใช้ `npm install` แทน

### 2. Permission denied
**แก้ไข:** ตรวจสอบว่าแชร์ Google Sheet ให้ Service Account แล้ว

### 3. API not enabled
**แก้ไข:** เปิดใช้งาน Google Sheets API ใน Google Cloud Console

### 4. Frontend ไม่แสดงข้อมูล
**แก้ไข:** 
- ตรวจสอบว่า Backend รันที่ port 3001
- ตรวจสอบ `NEXT_PUBLIC_API_URL` ใน `.env`

