# ✅ โปรเจกต์พร้อมใช้งานแล้ว!

**วันที่ตั้งค่า:** $(date)

---

## 🎉 สิ่งที่ทำเสร็จแล้ว

### ✅ Dependencies
- ✅ API dependencies ติดตั้งแล้ว (`api/node_modules`)
- ✅ UI dependencies ติดตั้งแล้ว (`ui/node_modules`)

### ✅ Google Sheets Configuration
- ✅ Environment variables ตั้งค่าแล้ว
  - ✅ `GOOGLE_SERVICE_ACCOUNT_EMAIL`
  - ✅ `GOOGLE_PRIVATE_KEY`
  - ✅ `GOOGLE_SPREADSHEET_ID`
- ✅ เชื่อมต่อ Google Sheets สำเร็จ
  - ✅ Spreadsheet: "Restaurant Management"
- ✅ Google Sheets Sheets สร้างแล้ว
  - ✅ **Stock** - มี headers: ชื่อสินค้า, จำนวน, ราคาต่อจำนวน, ผู้เพิ่มสต็อก, วันที่สร้าง
  - ✅ **Menu** - มี headers: ชื่ออาหาร, วัตถุดิบ, ราคาขาย, วันที่สร้าง
  - ✅ **Orders** - มี headers: รายการอาหาร, ยอดขาย, ต้นทุน, กำไร, วันที่สร้าง

---

## 🚀 วิธีรันแอปพลิเคชัน

### วิธีที่ 1: ใช้ Docker (แนะนำ)

```bash
docker-compose up --build
```

### วิธีที่ 2: รันแบบ Development

**Terminal 1 - Backend:**
```bash
cd api
pnpm run dev
```

**Terminal 2 - Frontend:**
```bash
cd ui
pnpm run dev
```

### วิธีที่ 3: ใช้ Makefile

```bash
make dev
```

---

## 🌐 URLs

เมื่อรันแอปพลิเคชันแล้ว:

- **Frontend (UI):** http://localhost:3000
- **Backend API:** http://localhost:3001
- **Health Check:** http://localhost:3001/health

---

## 📋 API Endpoints

### Stock (สต็อก)
- `GET /api/stock` - ดึงข้อมูลสต็อกทั้งหมด
- `POST /api/stock` - เพิ่มสต็อก
- `PUT /api/stock/:id` - แก้ไขสต็อก
- `DELETE /api/stock/:id` - ลบสต็อก

### Menu (เมนู)
- `GET /api/menu` - ดึงข้อมูลเมนูทั้งหมด
- `POST /api/menu` - เพิ่มเมนู
- `PUT /api/menu/:id` - แก้ไขเมนู
- `DELETE /api/menu/:id` - ลบเมนู

### Order (คำสั่งซื้อ)
- `GET /api/order` - ดึงข้อมูลคำสั่งซื้อทั้งหมด
- `POST /api/order` - สร้างคำสั่งซื้อ
- `GET /api/order/dashboard` - ดึงข้อมูลแดชบอร์ด

---

## 🔍 ตรวจสอบสถานะ

รันคำสั่งนี้เพื่อตรวจสอบสถานะ:

```bash
./check-status.sh
```

หรือตรวจสอบ Google Sheets:

```bash
cd api
pnpm run check-setup
```

---

## 📝 หมายเหตุ

- ข้อมูลทั้งหมดจะถูกเก็บใน Google Sheets ชื่อ "Restaurant Management"
- ข้อมูลจะถูกบันทึกแบบ real-time ลง Google Sheets
- Service Account: `restaurant-api-service@restaurant-management-480003.iam.gserviceaccount.com`
- Spreadsheet ID: `1_3pbkw3F7oayXKyY518n9LBKe6T-lXcBiXid5c_MNJk`

---

## 🆘 ถ้ามีปัญหา

### Backend ไม่รัน
```bash
cd api
pnpm run check-setup
```

### Frontend ไม่แสดงข้อมูล
- ตรวจสอบว่า Backend รันที่ port 3001
- ตรวจสอบ `NEXT_PUBLIC_API_URL` ใน `.env`

### ไม่สามารถเชื่อมต่อ Google Sheets
```bash
cd api
pnpm run check-setup
```
ตรวจสอบว่า:
1. Service Account มีสิทธิ์เข้าถึง Spreadsheet
2. Google Sheets API เปิดใช้งานแล้ว
3. Credentials ใน `.env` ถูกต้อง

---

## 🎊 พร้อมใช้งานแล้ว!

เปิดเบราว์เซอร์ไปที่ **http://localhost:3000** เพื่อเริ่มใช้งาน!

