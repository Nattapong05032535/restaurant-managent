# 📋 สรุปสิ่งที่ต้องตั้งค่าที่ Google Sheets และ Google Cloud

**สำหรับอีเมล:** `nattapong05032535@gmail.com`

---

## ✅ สิ่งที่ต้องทำทั้งหมด (4 ขั้นตอน)

### 1️⃣ สร้าง Google Cloud Service Account
**ทำที่:** [Google Cloud Console](https://console.cloud.google.com/)

**ขั้นตอน:**
1. ไปที่ Google Cloud Console (ใช้บัญชี `nattapong05032535@gmail.com`)
2. สร้างโปรเจกต์ใหม่ (หรือใช้โปรเจกต์ที่มีอยู่)
3. เปิดใช้งาน **Google Sheets API**:
   - ไปที่ "APIs & Services" > "Library"
   - ค้นหา "Google Sheets API"
   - คลิก "Enable"
4. สร้าง Service Account:
   - ไปที่ "APIs & Services" > "Credentials"
   - คลิก "Create Credentials" > "Service Account"
   - ตั้งชื่อ Service Account (เช่น `restaurant-api-service`)
   - คลิก "Done"
5. ดาวน์โหลด JSON Key:
   - คลิกที่ Service Account ที่สร้าง
   - ไปที่แท็บ "KEYS"
   - คลิก "Add Key" > "Create new key" > เลือก "JSON"
   - **บันทึกไฟล์ JSON นี้ไว้!**

**สิ่งที่ได้:**
- ✅ ไฟล์ JSON (เช่น `restaurant-management-xxxxx.json`)
- ✅ Service Account Email (อยู่ใน JSON file, หน้าตาแบบ: `restaurant-api-service@project.iam.gserviceaccount.com`)

---

### 2️⃣ สร้าง Google Sheet
**ทำที่:** [Google Sheets](https://sheets.google.com/)

**ขั้นตอน:**
1. ไปที่ Google Sheets (ใช้บัญชี `nattapong05032535@gmail.com`)
2. สร้าง Spreadsheet ใหม่
3. ตั้งชื่อ: `Restaurant Management` (หรือชื่ออื่นๆ)
4. **คัดลอก Spreadsheet ID** จาก URL:
   ```
   https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit
                                          ^^^^^^^^^^^^^^^^
                                          นี่คือ Spreadsheet ID
   ```

**สิ่งที่ได้:**
- ✅ Google Sheet ที่สร้างแล้ว
- ✅ Spreadsheet ID (จาก URL)

---

### 3️⃣ แชร์ Google Sheet ให้ Service Account
**ทำที่:** Google Sheet ที่สร้างไว้

**ขั้นตอน:**
1. เปิด Google Sheet ที่สร้างไว้
2. คลิกปุ่ม **"Share"** (มุมขวาบน)
3. ในช่อง "Add people and groups" ใส่:
   - **อีเมลของ Service Account** (จากไฟล์ JSON ที่ดาวน์โหลด, ดูที่ `client_email`)
   - ตัวอย่าง: `restaurant-api-service@restaurant-management.iam.gserviceaccount.com`
4. เลือกสิทธิ์เป็น **"Editor"**
5. **ยกเลิกการติ๊ก** "Notify people" (ไม่ต้องแจ้งเตือน)
6. คลิก **"Share"**

**⚠️ สำคัญมาก:** ถ้าไม่แชร์ให้ Service Account ระบบจะไม่สามารถเข้าถึง Google Sheet ได้!

**สิ่งที่ได้:**
- ✅ Google Sheet ที่แชร์ให้ Service Account แล้ว

---

### 4️⃣ ตั้งค่าไฟล์ .env ในโปรเจกต์
**ทำที่:** โปรเจกต์ `restaurant-managent`

**วิธีที่ 1: ใช้ Helper Script (แนะนำ - ง่ายที่สุด)**

```bash
# ให้สิทธิ์ execute
chmod +x scripts/setup-google-sheets.sh

# รัน script (แทนที่ path ด้วย path ของ JSON file ที่ดาวน์โหลดมา)
./scripts/setup-google-sheets.sh ~/Downloads/restaurant-management-xxxxx.json
```

Script จะถาม Spreadsheet ID และสร้างไฟล์ `.env` ให้อัตโนมัติ!

**วิธีที่ 2: ตั้งค่าด้วยตนเอง**

1. เปิดไฟล์ JSON ที่ดาวน์โหลดมา
2. เปิดไฟล์ `.env` ในโปรเจกต์
3. ใส่ข้อมูลต่อไปนี้:

```env
GOOGLE_SERVICE_ACCOUNT_EMAIL=restaurant-api-service@restaurant-management.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
GOOGLE_SPREADSHEET_ID=your-spreadsheet-id-here
```

**ข้อมูลที่ต้องใส่:**
- `GOOGLE_SERVICE_ACCOUNT_EMAIL` = ค่า `client_email` จากไฟล์ JSON
- `GOOGLE_PRIVATE_KEY` = ค่า `private_key` จากไฟล์ JSON (ต้องแปลง format)
- `GOOGLE_SPREADSHEET_ID` = Spreadsheet ID จาก URL ของ Google Sheet

**💡 ใช้ helper script เพื่อแปลง private key:**
```bash
node scripts/convert-private-key.js ~/Downloads/restaurant-management-xxxxx.json
```

**สิ่งที่ได้:**
- ✅ ไฟล์ `.env` ที่ตั้งค่าเรียบร้อยแล้ว

---

## 🎯 สรุป Checklist

ทำที่ Google Cloud Console:
- [ ] สร้างโปรเจกต์ (หรือใช้ที่มีอยู่)
- [ ] เปิดใช้งาน Google Sheets API
- [ ] สร้าง Service Account
- [ ] ดาวน์โหลด JSON key file

ทำที่ Google Sheets:
- [ ] สร้าง Google Sheet ใหม่
- [ ] คัดลอก Spreadsheet ID จาก URL
- [ ] แชร์ Google Sheet ให้ Service Account (Editor)

ทำที่โปรเจกต์:
- [ ] ตั้งค่าไฟล์ `.env` (ใช้ helper script หรือทำเอง)
- [ ] รัน `cd api && pnpm run create-sheets` เพื่อ initialize sheets
- [ ] รัน `docker-compose up --build` เพื่อเริ่มใช้งาน

---

## 📖 ดูรายละเอียดเพิ่มเติม

- **คู่มือละเอียด:** `SETUP_GOOGLE_SHEETS.md`
- **Quick Setup:** `QUICK_SETUP.md`

---

## ❓ คำถามที่พบบ่อย

**Q: ต้องสร้าง Service Account เองใช่ไหม?**  
A: ใช่ ต้องสร้างที่ Google Cloud Console

**Q: Google Sheet จะถูกสร้างอัตโนมัติไหม?**  
A: ไม่ Google Sheet ต้องสร้างเอง แต่ Sheets (Stock, Menu, Orders) จะถูกสร้างอัตโนมัติเมื่อรัน `pnpm run create-sheets`

**Q: ต้องแชร์ Google Sheet ให้ Service Account ไหม?**  
A: **ต้อง!** ไม่งั้นระบบจะเข้าถึงไม่ได้

**Q: ใช้บัญชี Google อะไรในการสร้าง?**  
A: ใช้บัญชี `nattapong05032535@gmail.com`

---

**🎉 เมื่อทำครบ 4 ขั้นตอนแล้ว ระบบจะพร้อมใช้งาน!**

