# 🎯 คู่มือตั้งค่าและเชื่อมต่อ Google Sheets ให้พร้อมใช้งาน

**สำหรับอีเมล:** `nattapong05032535@gmail.com`

---

## ⚠️ สิ่งสำคัญที่ต้องเข้าใจ

ฉันได้เตรียมเอกสารและสคริปต์ทั้งหมดให้คุณแล้ว **แต่** คุณต้องทำ **3 ขั้นตอน** ที่ Google เอง เพราะ:

1. ❌ **ฉันไม่สามารถสร้าง Service Account ให้คุณได้** - ต้องเข้าสู่ระบบ Google Cloud Console ด้วยบัญชีของคุณเอง
2. ❌ **ฉันไม่สามารถสร้าง Google Sheet ให้คุณได้** - ต้องเข้าสู่ระบบ Google Sheets ด้วยบัญชีของคุณเอง  
3. ❌ **ฉันไม่สามารถแชร์ Sheet ให้ Service Account ได้** - ต้องทำด้วยบัญชี Google ของคุณเอง

**แต่สิ่งที่ฉันทำให้แล้ว:**
- ✅ อัปเดตเอกสารทั้งหมดให้ใช้อีเมล `nattapong05032535@gmail.com`
- ✅ สร้างสคริปต์ช่วยตั้งค่าอัตโนมัติ
- ✅ สร้างคู่มือแบบละเอียดทุกขั้นตอน

---

## 🚀 วิธีที่ 1: ตั้งค่าแบบเร็ว (แนะนำ)

### ขั้นตอนที่ 1: สร้าง Service Account (10 นาที)

1. **ไปที่:** https://console.cloud.google.com/
2. **เข้าสู่ระบบด้วย:** `nattapong05032535@gmail.com`
3. **สร้างโปรเจกต์:**
   - คลิก "Select a project" > "New Project"
   - ชื่อ: `restaurant-management`
   - คลิก "Create"

4. **เปิดใช้งาน Google Sheets API:**
   - ไปที่ "APIs & Services" > "Library"
   - ค้นหา "Google Sheets API"
   - คลิก "Enable"

5. **สร้าง Service Account:**
   - ไปที่ "APIs & Services" > "Credentials"
   - คลิก "Create Credentials" > "Service Account"
   - ชื่อ: `restaurant-api-service`
   - คลิก "Done"

6. **ดาวน์โหลด JSON Key:**
   - คลิกที่ Service Account ที่สร้าง
   - ไปที่ "KEYS" > "Add Key" > "Create new key"
   - เลือก "JSON" > "Create"
   - **บันทึกไฟล์ JSON ที่ดาวน์โหลดมา!**

### ขั้นตอนที่ 2: สร้าง Google Sheet (2 นาที)

1. **ไปที่:** https://sheets.google.com/
2. **เข้าสู่ระบบด้วย:** `nattapong05032535@gmail.com`
3. **สร้าง Spreadsheet ใหม่:**
   - คลิก "Blank"
   - ตั้งชื่อ: `Restaurant Management`
4. **คัดลอก Spreadsheet ID** จาก URL:
   ```
   https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit
                                          ^^^^^^^^^^^^^^^^
                                          คัดลอกส่วนนี้
   ```

### ขั้นตอนที่ 3: แชร์ Google Sheet (1 นาที)

1. เปิด Google Sheet ที่สร้างไว้
2. คลิก "Share" (มุมขวาบน)
3. ใส่อีเมล Service Account (จาก JSON file, ดูที่ `client_email`)
4. เลือกสิทธิ์ "Editor"
5. คลิก "Share"

### ขั้นตอนที่ 4: ตั้งค่าในโปรเจกต์ (2 นาที)

```bash
# ใช้ helper script (จะตั้งค่าทุกอย่างให้อัตโนมัติ)
./scripts/setup-google-sheets.sh ~/Downloads/restaurant-management-xxxxx.json
```

Script จะถาม Spreadsheet ID และสร้างไฟล์ `.env` ให้อัตโนมัติ!

### ขั้นตอนที่ 5: Initialize และรัน

```bash
# ติดตั้ง dependencies (ถ้ายังไม่ติดตั้ง)
./install-all.sh

# Initialize Google Sheets (สร้าง Stock, Menu, Orders sheets)
cd api && pnpm run create-sheets

# ตรวจสอบว่าทุกอย่างถูกต้อง
pnpm run check-setup

# รันแอปพลิเคชัน
docker-compose up --build
```

---

## 📖 วิธีที่ 2: ดูคู่มือละเอียด

หากต้องการรายละเอียดเพิ่มเติม ให้อ่าน:

1. **`WHAT_TO_DO_AT_GOOGLE.md`** - สรุปสั้นๆ สิ่งที่ต้องทำที่ Google
2. **`SETUP_GOOGLE_SHEETS.md`** - คู่มือแบบละเอียดทุกขั้นตอน
3. **`QUICK_SETUP.md`** - คู่มือติดตั้งแบบเร็ว

---

## ✅ Checklist

### ที่ Google Cloud Console:
- [ ] สร้างโปรเจกต์
- [ ] เปิดใช้งาน Google Sheets API
- [ ] สร้าง Service Account
- [ ] ดาวน์โหลด JSON key file

### ที่ Google Sheets:
- [ ] สร้าง Google Sheet
- [ ] คัดลอก Spreadsheet ID
- [ ] แชร์ Sheet ให้ Service Account

### ที่โปรเจกต์:
- [ ] รัน `./scripts/setup-google-sheets.sh <json-file>`
- [ ] รัน `./install-all.sh`
- [ ] รัน `cd api && pnpm run create-sheets`
- [ ] รัน `docker-compose up --build`

---

## 🆘 ถ้ามีปัญหา

1. ตรวจสอบว่าแชร์ Google Sheet ให้ Service Account แล้ว
2. ตรวจสอบว่า Google Sheets API เปิดใช้งานแล้ว
3. ตรวจสอบว่าไฟล์ `.env` มีข้อมูลครบถ้วน
4. รัน `cd api && pnpm run check-setup` เพื่อตรวจสอบ

---

## 📞 ข้อมูลติดต่อ

**อีเมลที่ใช้:** `nattapong05032535@gmail.com`

---

**🎉 เมื่อทำครบทุกขั้นตอนแล้ว ระบบจะพร้อมใช้งาน!**

**เข้าถึงได้ที่:** http://localhost:3000

